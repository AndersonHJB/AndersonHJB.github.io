import{_ as e}from"./gzh-DnOBNg6W.js";import{_ as t}from"./zsxq-BcdwOI-_.js";import{_ as s}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as a,o,a as n}from"./app-CJkn7D_W.js";const i={},r=n(`<div style="text-align:center;"><p>Assignment 3</p><p>Due Tuesday, November 21 at 11:55pm</p><p>ith an automatic extension to Sunday, November 26 at 11:55pm</p></div><p>This is an assembly language programming assignment in which you have to write assembly code for inserting nodes containing employee information into a binary search tree and for removing nodes from the tree.</p><p>I have provided a substantial amount of C code, including C code for the functions that you will be writing in assembly. First, you need to download one of the attached compressed files appropriate for your computer: <strong>assignment3_macos.tgz</strong> for macOS, <strong>assignment3_cygwin.tgz</strong> for Windows/Cygwin, or <strong>assignment3_macos.tgz</strong> for Linux. As with the previous assignments, save and uncompress the downloaded file in the directory where you want to work on and compile your program. To uncompress the file, in a shell, type</p><p>tar -xzvf filename</p><p>where filename is the name of the file that you downloaded.</p><p>One of the resulting files is &quot;Assignment 3.pdf&quot;. It contains all the details of the assignment, along with some helpful hints.</p><p><strong>Please get started on this assignment right away!</strong> It is much more challenging than the small amount of assembly code you had to write for Assignment 2. As you can see, it is due the Tuesday before Thanksgiving, but you can have an automatic extension (without a penalty) until the Sunday of Thanksgiving weekend - but then don&#39;t blame me for ruining your holiday.</p><p>This assignment requires you to write two assembly language functions, one to insert employee records into a binary search tree and one to remove employee records from the binary search tree in alphabetical order. You are being given the C versions of these two functions, as well as the C code that uses these functions.</p><p>You should perform the following series of steps:</p><h2 id="step-1" tabindex="-1"><a class="header-anchor" href="#step-1"><span>Step 1</span></a></h2><p>If you are reading this document, it means that you have already downloaded and unzipped the file containing the source code and input files. You should have the following files: <code>assignment3.c</code>, <code>bin.s</code>, <code>binary_search_tree.c</code>, <code>binary_search_tree.h</code>, <code>input_file.txt</code>, and makefile.</p><p>Compile the provided code (in a shell) by typing “make”. This produces an executable file called “assign3”. To run the executable, type:</p><p><code>./assign3 input_file.txt output_file.txt</code></p><p>This should create a file named output_file.txt whose first line is:</p><p><code>Abarry, Abdou, 599872805, 194617</code></p><p>and whose last line is:</p><p><code>Zeledon, Rodrigo, 1144067779, 70062</code></p><h2 id="step-2" tabindex="-1"><a class="header-anchor" href="#step-2"><span>Step 2</span></a></h2><p>Read the file binary_search_tree.h closely. You’ll see that it defines a struct type EMPLOYEE, which has an 8-byte (i.e. long int) “id” field, an 8-byte “salary” field, a 100-byte “first” field (a character array for holding the first name), and a 100-byte “last” field (for holding the last name).</p><p>Thus, if a register such as %rcx points to an EMPLOYEE struct (which starts with the “id” field), the fields of the EMPLOYEE would be accessed as offsets from %rcx as follows (where you should fill in the offset in each “___” based on writing and running a tiny C program, like my offset.c on Brightspace, that tells you the offsets).</p><p>EMPLOYEE (216 bytes)</p><table><thead><tr><th>Field</th><th>Size</th><th>Offset</th></tr></thead><tbody><tr><td>last</td><td>100 bytes</td><td>____(%rcx)</td></tr><tr><td>first</td><td>100 bytes</td><td>____(%rcx)</td></tr><tr><td>salary</td><td>8 bytes</td><td>____(%rcx)</td></tr><tr><td>id</td><td>8 bytes</td><td>____(%rcx)</td></tr></tbody></table><p>You will also see in binary_search_tree.h the definition of a NODE struct type, which will be used for the binary search tree. A NODE has a “person” field of type EMPLOYEE (above), and two pointer fields, “left” and “right”, each of type NODE *. If, for example, %rcx points to a NODE, then the fields of the NODE would be accessed as offsets from %rcx as follows (where you should fill in the offsets in “___”).</p><p>NODE (232 bytes)</p><table><thead><tr><th>Field</th><th>Size</th><th>Offset</th></tr></thead><tbody><tr><td>right</td><td>8 bytes</td><td>___(%rcx)</td></tr><tr><td>left</td><td>8 bytes</td><td>___(%rcx)</td></tr><tr><td>person</td><td>216 bytes</td><td>___(%rcx)</td></tr></tbody></table><p>The NODE type will be used to define the nodes of a binary search tree. As you probably recall, a binary search tree is a tree where the label associated with each internal node is greater than any label found in its left subtree (if there is one) and less than any label found in its right subtree (if there is one). For example, the following tree is a binary search tree.</p><div class="language-c line-numbers-mode" data-highlighter="shiki" data-ext="c" data-title="c" style="background-color:#272822;color:#F8F8F2;"><pre class="shiki monokai vp-code"><code><span class="line"><span style="color:#AE81FF;">        8</span></span>
<span class="line"><span style="color:#F92672;">       /</span><span style="color:#AE81FF;"> \\</span></span>
<span class="line"><span style="color:#F92672;">      /</span><span style="color:#AE81FF;">   \\</span></span>
<span class="line"><span style="color:#AE81FF;">     4</span><span style="color:#AE81FF;">    15</span></span>
<span class="line"><span style="color:#F92672;">    /</span><span style="color:#F8F8F2;"> \\   </span><span style="color:#F92672;">/</span><span style="color:#AE81FF;"> \\</span></span>
<span class="line"><span style="color:#AE81FF;">   2</span><span style="color:#AE81FF;">   6</span><span style="color:#AE81FF;"> 11</span><span style="color:#AE81FF;">  19</span></span>
<span class="line"><span style="color:#F8F8F2;">        \\     </span><span style="color:#AE81FF;">\\</span></span>
<span class="line"><span style="color:#AE81FF;">         5</span><span style="color:#AE81FF;">     13</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In this assignment, the employee names (not numbers, as shown above) will be the basis for the ordering of the nodes in the tree. Note that the code for this assignment makes no attempt to balance the tree. That is, it makes no attempt to ensure that the paths from the root to the leaves are roughly the same length (which would lead to a more efficient search since that would minimize the height of the tree).</p><h2 id="step-3" tabindex="-1"><a class="header-anchor" href="#step-3"><span>Step 3</span></a></h2><p>Read <code>binary_search_tree.c</code> very closely. As indicated by the code and comments in the file, the following functions are defined:</p><ul><li><code>new_node()</code>: This function takes an employee record as a parameter, allocates a new node (of type NODE) by calling <code>malloc()</code>, populates the node with the employee data, and returns a pointer to the node.</li><li><code>insert_node()</code>: This function take a node containing employee data as a parameter and inserts the node into the binary search tree whose root is pointed to by the global variable “root”. The insertion, of course, maintains the tree as a binary search tree. The employee’s name is used as the basis for determining where in the tree the node is inserted.</li><li><code>remove_smallest()</code>: This function removes the smallest (alphabetically) node from the binary search tree. The node removed, of course, is the leftmost node in the tree. The function returns the removed node.</li><li><code>insert_employee()</code>: This function takes an employee record as a parameter and calls <code>new_node()</code> and <code>insert_node()</code> to insert a new node containing the specified employee data into the binary search tree.</li><li><code>remove_employee()</code>: This function takes a pointer to an employee record as a parameter. It calls <code>remove_smallest()</code> to remove the smallest (alphabetically) node from the binary search tree, and copies the employee data from that node into the record pointed to by the parameter.</li></ul><h2 id="step-4" tabindex="-1"><a class="header-anchor" href="#step-4"><span>Step 4</span></a></h2><p>Read <code>assignment3.c</code> closely. As the comments and code indicates, the <code>main()</code> function uses the functions in <code>binary_search_tree.c</code> to sort employee data. In particular, <code>main()</code> expects two command-line parameters in <code>argv[]</code>: the name of a file to read unsorted employee data from and the name of a file to write the sorted employee data to. It reads the employee data into an EMPLOYEE array named <code>unsorted_employees</code>. It then sorts the employee records alphabetically by putting each employee record into a node and inserting the node into a binary search tree. Once all the employees are in the binary search tree, the smallest (alphabetically) employee record is repeatedly removed from the tree and placed into the array <code>sorted_employees</code> (in successive positions), until the tree is empty. At this point, the employee records in <code>sorted_employees</code> have been sorted and are then written to the specified output file.</p><h2 id="step-5" tabindex="-1"><a class="header-anchor" href="#step-5"><span>Step 5</span></a></h2><p>Review the calling convention information for macOS that was provided on Brightspace. Be sure to also review the assembly language programs written in class that demonstrate the use of calling conventions.</p><h2 id="step-6" tabindex="-1"><a class="header-anchor" href="#step-6"><span>Step 6</span></a></h2><p>This is the step where you write the assembly code for the insert_node() and remove_smallest() functions (only). You should put your assembly code in the file bin.s that has been provided for you. You should:</p><ol><li>Comment out the code for <code>insert_node()</code> in <code>binary_search_tree.c</code> (or simply rename the function to another name). Commented-out C code for <code>insert_node()</code> has already been put in bin.s. Your job is to translate it into assembly code. You can insert the assembly code corresponding to a line of C code right after that line of C code, so that commented-out C code and assembly code is interleaved.</li><li>Once you have finished writing the assembly code for <code>insert_node()</code>, you should debug it before moving on to writing <code>remove_smallest()</code>.</li><li>Repeat step 1 for <code>remove_smallest()</code>.</li></ol><p>Here are some helpful hints:</p><ul><li>The “root” global variable, which is already defined in <code>binary_search_tree.c</code>, can be accessed in assembly language as “<code>_root(%rip)</code>”.</li><li>The <code>strcmp()</code> function, which is called by <code>insert_node()</code>, expects its two parameters to be addresses – in this case, the addresses of character arrays containing employee names. In order to pass these addresses to <code>strcmp()</code>, use the “leaq” instruction to compute each address.</li><li>Make sure, in <code>insert_node()</code>, to save (push) the caller-saved registers that you are using before you call <code>strcmp()</code>, and then restore (pop) them after the call (in reverse order).</li><li>If you are using any callee-saved registers in <code>insert_node()</code> or <code>remove_smallest()</code>, you’ll need to save those registers prior to the first time you use them and restore those registers after you’re done using them.</li><li>As discussed in class, keep %rsp aligned on 16-byte boundaries by always decrementing %rsp by a multiple of 16. Since the push instruction pushes 8 bytes onto to the stack, always either perform an even number of pushes or, if you have an odd number of pushes, subtract 8 from %rsp after doing a push.</li></ul><h2 id="step-7" tabindex="-1"><a class="header-anchor" href="#step-7"><span>Step 7</span></a></h2><p>When your assembly code in bin.s is working, submit only the bin.s file by uploading it to Brightspace.</p><p>欢迎关注我公众号：AI悦创，有更多更好玩的等你发现！</p><details class="hint-container details"><summary>公众号：AI悦创【二维码】</summary><figure><img src="`+e+'" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure></details><div class="hint-container info"><p class="hint-container-title">AI悦创·编程一对一</p><p>AI悦创·推出辅导班啦，包括「Python 语言辅导班、C++ 辅导班、java 辅导班、算法/数据结构辅导班、少儿编程、pygame 游戏开发」，全部都是一对一教学：一对一辅导 + 一对一答疑 + 布置作业 + 项目实践等。当然，还有线下线上摄影课程、Photoshop、Premiere 一对一教学、QQ、微信在线，随时响应！微信：Jiabcdefh</p><p>C++ 信息奥赛题解，长期更新！长期招收一对一中小学信息奥赛集训，莆田、厦门地区有机会线下上门，其他地区线上。微信：Jiabcdefh</p><p>方法一：<a href="http://wpa.qq.com/msgrd?v=3&amp;uin=1432803776&amp;site=qq&amp;menu=yes" target="_blank" rel="noopener noreferrer">QQ</a></p><p>方法二：微信：Jiabcdefh</p></div><figure><img src="'+t+'" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure>',46),l=[r];function d(c,h){return o(),a("div",null,l)}const y=s(i,[["render",d],["__file","39-ProgrammingAssignment3.html.vue"]]),g=JSON.parse('{"path":"/1v1/06-KAI/39-ProgrammingAssignment3.html","title":"Programming Assignment 3","lang":"zh-CN","frontmatter":{"title":"Programming Assignment 3","icon":"c","date":"2023-11-20T23:20:31.000Z","author":"AI悦创","isOriginal":true,"category":["1v1","java 1v1","纽约大学一对一","NYU 1v1","web 一对一","数据结构一对一","留学生辅导","留学生作业辅导"],"tag":["纽约大学一对一","NYU 1v1"],"sticky":false,"star":false,"article":true,"timeline":true,"image":false,"navbar":true,"sidebarIcon":true,"headerDepth":5,"comment":true,"lastUpdated":true,"editLink":false,"backToTop":true,"toc":true,"feed":false,"seo":false,"head":[]},"headers":[{"level":2,"title":"Step 1","slug":"step-1","link":"#step-1","children":[]},{"level":2,"title":"Step 2","slug":"step-2","link":"#step-2","children":[]},{"level":2,"title":"Step 3","slug":"step-3","link":"#step-3","children":[]},{"level":2,"title":"Step 4","slug":"step-4","link":"#step-4","children":[]},{"level":2,"title":"Step 5","slug":"step-5","link":"#step-5","children":[]},{"level":2,"title":"Step 6","slug":"step-6","link":"#step-6","children":[]},{"level":2,"title":"Step 7","slug":"step-7","link":"#step-7","children":[]}],"git":{"createdTime":1700498123000,"updatedTime":1700535693000,"contributors":[{"name":"AndersonHJB","email":"cleland1432803776@icloud.com","commits":3}]},"readingTime":{"minutes":6.17,"words":1851},"localizedDate":"2023年11月21日","copyright":{"author":"AI悦创"}}');export{y as comp,g as data};
