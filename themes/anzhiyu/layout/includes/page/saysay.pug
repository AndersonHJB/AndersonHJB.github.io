#saysay-box
    - let saysay_background = page.top_background
    .author-content.author-content-item.saysay.single(style=saysay_background ? `background: url(${saysay_background}) top / cover no-repeat` : "")
        .card-content
            .author-content-item-tips 每一瞬间的思想，都应该被捕获
            span.author-content-item-title 悦说
            .content-bottom
            .tips 记录本身，即已是反抗！
            .banner-button-group
              a.banner-button(onclick='pjax.loadUrl("/about/")')
                i.anzhiyufont.anzhiyu-icon-arrow-circle-right(style='font-size: 1.5rem')
                span.banner-button-text 我的更多
    #saysay-main
      each group in site.data.saysay.sayings
        .group
          h2= group.group
          each item in group.items
            .say-item
              .header
                img.avatar(src='https://blog.bornforthis.cn/img/favicon.svg', alt='Avatar')
                .nickname Bornforthis
                .time= item.time
              .content!= item.content
              if item.image || item.video
                .media-grid
                  if item.image
                    each image in item.image
                      a.media-item(href=image, data-fancybox)
                        img(src=image, alt='Image')
                  if item.video
                    each video in item.video
                      a.media-item(href=video, data-fancybox)
                        img(src='path/to/video_thumbnail.jpg', alt='Video')
